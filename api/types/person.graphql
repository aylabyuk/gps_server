scalar Date

type Person {
    id: ID!
    firstName: String
    lastName: String
    nickName: String
    birthdate: Date
    position: Position
    division: Division
    email: [Email]
    contactNumber: [ContactNumber]
}

type Query {
    singlePerson(id: ID!): Person
    allPerson: [Person]
}