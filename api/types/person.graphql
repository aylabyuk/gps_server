scalar Date

type Staff {
    id: ID!
    firstName: String
    lastName: String
    nickName: String
    birthdate: Date
    position: Position
    division: Division
    email: [Email]
    contactNumber: [ContactNumber]
}

type Query {
    singleStaff(id: ID!): Staff
    allStaff: [Staff]
}